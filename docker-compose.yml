services:
  mongo:
    image: mongo:7
    container_name: chatpdf-mongo
    ports:
      - "27017:27017"
    volumes:
      - mongo-data:/data/db

  redis:
    image: redis:7
    container_name: chatpdf-redis
    ports:
      - "6379:6379"

volumes:
  mongo-data: